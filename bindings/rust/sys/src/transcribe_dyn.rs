// @generated by `cargo xtask bindgen` from include/transcribe/extensions.h
// DO NOT EDIT BY HAND. Regenerate: `cargo xtask bindgen`.
// Pinned to include/transcribe.abihash = 7896d8d4c2a46147

/// The public-ABI digest these bindings were generated against
/// (sha256/16 over the normalized FFI surface). The load-time version
/// gate and the CI drift check both anchor on this value.
pub const PUBLIC_HEADER_HASH: &str = "7896d8d4c2a46147";

/* automatically generated by rust-bindgen 0.72.1 */

pub const TRANSCRIBE_EXT_KIND_MOONSHINE_STREAMING_STREAM: u32 = 1414746957;
pub const TRANSCRIBE_EXT_KIND_PARAKEET_STREAM: u32 = 1414744912;
pub const TRANSCRIBE_EXT_KIND_PARAKEET_BUFFERED_STREAM: u32 = 1396853584;
pub const TRANSCRIBE_EXT_KIND_SORTFORMER_STREAM: u32 = 1414743635;
pub const TRANSCRIBE_EXT_KIND_VOXTRAL_REALTIME_STREAM: u32 = 1414746710;
pub const TRANSCRIBE_EXT_KIND_WHISPER_RUN: u32 = 1314015319;
impl transcribe_status {
    pub const TRANSCRIBE_OK: transcribe_status = transcribe_status(0);
    pub const TRANSCRIBE_ERR_INVALID_ARG: transcribe_status = transcribe_status(1);
    pub const TRANSCRIBE_ERR_NOT_IMPLEMENTED: transcribe_status = transcribe_status(2);
    pub const TRANSCRIBE_ERR_FILE_NOT_FOUND: transcribe_status = transcribe_status(3);
    pub const TRANSCRIBE_ERR_GGUF: transcribe_status = transcribe_status(4);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_ARCH: transcribe_status = transcribe_status(5);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_VARIANT: transcribe_status = transcribe_status(6);
    pub const TRANSCRIBE_ERR_OOM: transcribe_status = transcribe_status(7);
    pub const TRANSCRIBE_ERR_BACKEND: transcribe_status = transcribe_status(8);
    pub const TRANSCRIBE_ERR_SAMPLE_RATE: transcribe_status = transcribe_status(9);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_LANGUAGE: transcribe_status = transcribe_status(10);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_TASK: transcribe_status = transcribe_status(11);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_TIMESTAMPS: transcribe_status = transcribe_status(12);
    pub const TRANSCRIBE_ERR_ABORTED: transcribe_status = transcribe_status(13);
    pub const TRANSCRIBE_ERR_BAD_STRUCT_SIZE: transcribe_status = transcribe_status(14);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_PNC: transcribe_status = transcribe_status(15);
    pub const TRANSCRIBE_ERR_UNSUPPORTED_ITN: transcribe_status = transcribe_status(16);
    pub const TRANSCRIBE_ERR_INPUT_TOO_LONG: transcribe_status = transcribe_status(17);
    pub const TRANSCRIBE_ERR_OUTPUT_TRUNCATED: transcribe_status = transcribe_status(18);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_status(pub ::std::os::raw::c_uint);



impl transcribe_abi_struct {
    pub const TRANSCRIBE_ABI_MODEL_LOAD_PARAMS: transcribe_abi_struct = transcribe_abi_struct(0);
    pub const TRANSCRIBE_ABI_SESSION_PARAMS: transcribe_abi_struct = transcribe_abi_struct(1);
    pub const TRANSCRIBE_ABI_RUN_PARAMS: transcribe_abi_struct = transcribe_abi_struct(2);
    pub const TRANSCRIBE_ABI_STREAM_PARAMS: transcribe_abi_struct = transcribe_abi_struct(3);
    pub const TRANSCRIBE_ABI_CAPABILITIES: transcribe_abi_struct = transcribe_abi_struct(4);
    pub const TRANSCRIBE_ABI_TIMINGS: transcribe_abi_struct = transcribe_abi_struct(5);
    pub const TRANSCRIBE_ABI_SEGMENT: transcribe_abi_struct = transcribe_abi_struct(6);
    pub const TRANSCRIBE_ABI_WORD: transcribe_abi_struct = transcribe_abi_struct(7);
    pub const TRANSCRIBE_ABI_TOKEN: transcribe_abi_struct = transcribe_abi_struct(8);
    pub const TRANSCRIBE_ABI_STREAM_UPDATE: transcribe_abi_struct = transcribe_abi_struct(9);
    pub const TRANSCRIBE_ABI_STREAM_TEXT: transcribe_abi_struct = transcribe_abi_struct(10);
    pub const TRANSCRIBE_ABI_SESSION_LIMITS: transcribe_abi_struct = transcribe_abi_struct(11);
    pub const TRANSCRIBE_ABI_EXT: transcribe_abi_struct = transcribe_abi_struct(12);
    pub const TRANSCRIBE_ABI_BACKEND_DEVICE: transcribe_abi_struct = transcribe_abi_struct(13);
    pub const TRANSCRIBE_ABI_SPEAKER_SEGMENT: transcribe_abi_struct = transcribe_abi_struct(14);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_abi_struct(pub ::std::os::raw::c_uint);


impl transcribe_log_level {
    pub const TRANSCRIBE_LOG_LEVEL_NONE: transcribe_log_level = transcribe_log_level(0);
    pub const TRANSCRIBE_LOG_LEVEL_INFO: transcribe_log_level = transcribe_log_level(1);
    pub const TRANSCRIBE_LOG_LEVEL_WARN: transcribe_log_level = transcribe_log_level(2);
    pub const TRANSCRIBE_LOG_LEVEL_ERROR: transcribe_log_level = transcribe_log_level(3);
    pub const TRANSCRIBE_LOG_LEVEL_DEBUG: transcribe_log_level = transcribe_log_level(4);
    pub const TRANSCRIBE_LOG_LEVEL_CONT: transcribe_log_level = transcribe_log_level(5);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_log_level(pub ::std::os::raw::c_uint);
pub type transcribe_log_callback = ::std::option::Option<
    unsafe extern "C" fn(
        level: transcribe_log_level,
        msg: *const ::std::os::raw::c_char,
        userdata: *mut ::std::os::raw::c_void,
    ),
>;

impl transcribe_task {
    pub const TRANSCRIBE_TASK_TRANSCRIBE: transcribe_task = transcribe_task(0);
    pub const TRANSCRIBE_TASK_TRANSLATE: transcribe_task = transcribe_task(1);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_task(pub ::std::os::raw::c_uint);
impl transcribe_timestamp_kind {
    pub const TRANSCRIBE_TIMESTAMPS_NONE: transcribe_timestamp_kind = transcribe_timestamp_kind(0);
    pub const TRANSCRIBE_TIMESTAMPS_AUTO: transcribe_timestamp_kind = transcribe_timestamp_kind(1);
    pub const TRANSCRIBE_TIMESTAMPS_SEGMENT: transcribe_timestamp_kind =
        transcribe_timestamp_kind(2);
    pub const TRANSCRIBE_TIMESTAMPS_WORD: transcribe_timestamp_kind = transcribe_timestamp_kind(3);
    pub const TRANSCRIBE_TIMESTAMPS_TOKEN: transcribe_timestamp_kind = transcribe_timestamp_kind(4);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_timestamp_kind(pub ::std::os::raw::c_uint);
impl transcribe_kv_type {
    pub const TRANSCRIBE_KV_TYPE_AUTO: transcribe_kv_type = transcribe_kv_type(0);
    pub const TRANSCRIBE_KV_TYPE_F32: transcribe_kv_type = transcribe_kv_type(1);
    pub const TRANSCRIBE_KV_TYPE_F16: transcribe_kv_type = transcribe_kv_type(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_kv_type(pub ::std::os::raw::c_uint);
impl transcribe_pnc_mode {
    pub const TRANSCRIBE_PNC_MODE_DEFAULT: transcribe_pnc_mode = transcribe_pnc_mode(0);
    pub const TRANSCRIBE_PNC_MODE_OFF: transcribe_pnc_mode = transcribe_pnc_mode(1);
    pub const TRANSCRIBE_PNC_MODE_ON: transcribe_pnc_mode = transcribe_pnc_mode(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_pnc_mode(pub ::std::os::raw::c_uint);
impl transcribe_itn_mode {
    pub const TRANSCRIBE_ITN_MODE_DEFAULT: transcribe_itn_mode = transcribe_itn_mode(0);
    pub const TRANSCRIBE_ITN_MODE_OFF: transcribe_itn_mode = transcribe_itn_mode(1);
    pub const TRANSCRIBE_ITN_MODE_ON: transcribe_itn_mode = transcribe_itn_mode(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_itn_mode(pub ::std::os::raw::c_uint);
impl transcribe_diarize_mode {
    pub const TRANSCRIBE_DIARIZE_MODE_DEFAULT: transcribe_diarize_mode = transcribe_diarize_mode(0);
    pub const TRANSCRIBE_DIARIZE_MODE_OFF: transcribe_diarize_mode = transcribe_diarize_mode(1);
    pub const TRANSCRIBE_DIARIZE_MODE_ON: transcribe_diarize_mode = transcribe_diarize_mode(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_diarize_mode(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_model {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_session {
    _unused: [u8; 0],
}
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_ext {
    pub size: u64,
    pub kind: u32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_ext"][::std::mem::size_of::<transcribe_ext>() - 16usize];
    ["Alignment of transcribe_ext"][::std::mem::align_of::<transcribe_ext>() - 8usize];
    ["Offset of field: transcribe_ext::size"]
        [::std::mem::offset_of!(transcribe_ext, size) - 0usize];
    ["Offset of field: transcribe_ext::kind"]
        [::std::mem::offset_of!(transcribe_ext, kind) - 8usize];
};

impl transcribe_ext_slot {
    pub const TRANSCRIBE_EXT_SLOT_RUN: transcribe_ext_slot = transcribe_ext_slot(0);
    pub const TRANSCRIBE_EXT_SLOT_STREAM: transcribe_ext_slot = transcribe_ext_slot(1);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_ext_slot(pub ::std::os::raw::c_uint);

impl transcribe_backend_request {
    pub const TRANSCRIBE_BACKEND_AUTO: transcribe_backend_request = transcribe_backend_request(0);
    pub const TRANSCRIBE_BACKEND_CPU: transcribe_backend_request = transcribe_backend_request(1);
    pub const TRANSCRIBE_BACKEND_METAL: transcribe_backend_request = transcribe_backend_request(2);
    pub const TRANSCRIBE_BACKEND_VULKAN: transcribe_backend_request = transcribe_backend_request(3);
    pub const TRANSCRIBE_BACKEND_CPU_ACCEL: transcribe_backend_request =
        transcribe_backend_request(4);
    pub const TRANSCRIBE_BACKEND_CUDA: transcribe_backend_request = transcribe_backend_request(5);
    pub const TRANSCRIBE_BACKEND_ROCM: transcribe_backend_request = transcribe_backend_request(6);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_backend_request(pub ::std::os::raw::c_uint);



impl transcribe_device_type {
    pub const TRANSCRIBE_DEVICE_TYPE_CPU: transcribe_device_type = transcribe_device_type(0);
    pub const TRANSCRIBE_DEVICE_TYPE_GPU: transcribe_device_type = transcribe_device_type(1);
    pub const TRANSCRIBE_DEVICE_TYPE_IGPU: transcribe_device_type = transcribe_device_type(2);
    pub const TRANSCRIBE_DEVICE_TYPE_ACCEL: transcribe_device_type = transcribe_device_type(3);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_device_type(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_backend_device {
    pub struct_size: u64,
    pub name: *const ::std::os::raw::c_char,
    pub description: *const ::std::os::raw::c_char,
    pub kind: *const ::std::os::raw::c_char,
    pub device_id: *const ::std::os::raw::c_char,
    pub memory_total: u64,
    pub memory_free: u64,
    pub device_type: transcribe_device_type,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_backend_device"]
        [::std::mem::size_of::<transcribe_backend_device>() - 64usize];
    ["Alignment of transcribe_backend_device"]
        [::std::mem::align_of::<transcribe_backend_device>() - 8usize];
    ["Offset of field: transcribe_backend_device::struct_size"]
        [::std::mem::offset_of!(transcribe_backend_device, struct_size) - 0usize];
    ["Offset of field: transcribe_backend_device::name"]
        [::std::mem::offset_of!(transcribe_backend_device, name) - 8usize];
    ["Offset of field: transcribe_backend_device::description"]
        [::std::mem::offset_of!(transcribe_backend_device, description) - 16usize];
    ["Offset of field: transcribe_backend_device::kind"]
        [::std::mem::offset_of!(transcribe_backend_device, kind) - 24usize];
    ["Offset of field: transcribe_backend_device::device_id"]
        [::std::mem::offset_of!(transcribe_backend_device, device_id) - 32usize];
    ["Offset of field: transcribe_backend_device::memory_total"]
        [::std::mem::offset_of!(transcribe_backend_device, memory_total) - 40usize];
    ["Offset of field: transcribe_backend_device::memory_free"]
        [::std::mem::offset_of!(transcribe_backend_device, memory_free) - 48usize];
    ["Offset of field: transcribe_backend_device::device_type"]
        [::std::mem::offset_of!(transcribe_backend_device, device_type) - 56usize];
};




#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_model_load_params {
    pub struct_size: u64,
    pub backend: transcribe_backend_request,
    pub gpu_device: ::std::os::raw::c_int,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_model_load_params"]
        [::std::mem::size_of::<transcribe_model_load_params>() - 16usize];
    ["Alignment of transcribe_model_load_params"]
        [::std::mem::align_of::<transcribe_model_load_params>() - 8usize];
    ["Offset of field: transcribe_model_load_params::struct_size"]
        [::std::mem::offset_of!(transcribe_model_load_params, struct_size) - 0usize];
    ["Offset of field: transcribe_model_load_params::backend"]
        [::std::mem::offset_of!(transcribe_model_load_params, backend) - 8usize];
    ["Offset of field: transcribe_model_load_params::gpu_device"]
        [::std::mem::offset_of!(transcribe_model_load_params, gpu_device) - 12usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_session_params {
    pub struct_size: u64,
    pub n_threads: ::std::os::raw::c_int,
    pub kv_type: transcribe_kv_type,
    pub n_ctx: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_session_params"]
        [::std::mem::size_of::<transcribe_session_params>() - 24usize];
    ["Alignment of transcribe_session_params"]
        [::std::mem::align_of::<transcribe_session_params>() - 8usize];
    ["Offset of field: transcribe_session_params::struct_size"]
        [::std::mem::offset_of!(transcribe_session_params, struct_size) - 0usize];
    ["Offset of field: transcribe_session_params::n_threads"]
        [::std::mem::offset_of!(transcribe_session_params, n_threads) - 8usize];
    ["Offset of field: transcribe_session_params::kv_type"]
        [::std::mem::offset_of!(transcribe_session_params, kv_type) - 12usize];
    ["Offset of field: transcribe_session_params::n_ctx"]
        [::std::mem::offset_of!(transcribe_session_params, n_ctx) - 16usize];
};

impl transcribe_vad_mode {
    pub const TRANSCRIBE_VAD_OFF: transcribe_vad_mode = transcribe_vad_mode(0);
    pub const TRANSCRIBE_VAD_SILERO: transcribe_vad_mode = transcribe_vad_mode(1);
    pub const TRANSCRIBE_VAD_ENERGY: transcribe_vad_mode = transcribe_vad_mode(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_vad_mode(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_vad_params {
    pub struct_size: u64,
    pub mode: transcribe_vad_mode,
    pub dll_path: *const ::std::os::raw::c_char,
    pub weight_path: *const ::std::os::raw::c_char,
    pub backend: ::std::os::raw::c_int,
    pub device_id: ::std::os::raw::c_int,
    pub n_threads: ::std::os::raw::c_int,
    pub max_chunk_ms: i64,
    pub merge_gap_ms: i64,
    pub padding_ms: i64,
    pub silero_threshold: f32,
    pub silero_min_speech_ms: i64,
    pub silero_min_silence_ms: i64,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_vad_params"][::std::mem::size_of::<transcribe_vad_params>() - 96usize];
    ["Alignment of transcribe_vad_params"]
        [::std::mem::align_of::<transcribe_vad_params>() - 8usize];
    ["Offset of field: transcribe_vad_params::struct_size"]
        [::std::mem::offset_of!(transcribe_vad_params, struct_size) - 0usize];
    ["Offset of field: transcribe_vad_params::mode"]
        [::std::mem::offset_of!(transcribe_vad_params, mode) - 8usize];
    ["Offset of field: transcribe_vad_params::dll_path"]
        [::std::mem::offset_of!(transcribe_vad_params, dll_path) - 16usize];
    ["Offset of field: transcribe_vad_params::weight_path"]
        [::std::mem::offset_of!(transcribe_vad_params, weight_path) - 24usize];
    ["Offset of field: transcribe_vad_params::backend"]
        [::std::mem::offset_of!(transcribe_vad_params, backend) - 32usize];
    ["Offset of field: transcribe_vad_params::device_id"]
        [::std::mem::offset_of!(transcribe_vad_params, device_id) - 36usize];
    ["Offset of field: transcribe_vad_params::n_threads"]
        [::std::mem::offset_of!(transcribe_vad_params, n_threads) - 40usize];
    ["Offset of field: transcribe_vad_params::max_chunk_ms"]
        [::std::mem::offset_of!(transcribe_vad_params, max_chunk_ms) - 48usize];
    ["Offset of field: transcribe_vad_params::merge_gap_ms"]
        [::std::mem::offset_of!(transcribe_vad_params, merge_gap_ms) - 56usize];
    ["Offset of field: transcribe_vad_params::padding_ms"]
        [::std::mem::offset_of!(transcribe_vad_params, padding_ms) - 64usize];
    ["Offset of field: transcribe_vad_params::silero_threshold"]
        [::std::mem::offset_of!(transcribe_vad_params, silero_threshold) - 72usize];
    ["Offset of field: transcribe_vad_params::silero_min_speech_ms"]
        [::std::mem::offset_of!(transcribe_vad_params, silero_min_speech_ms) - 80usize];
    ["Offset of field: transcribe_vad_params::silero_min_silence_ms"]
        [::std::mem::offset_of!(transcribe_vad_params, silero_min_silence_ms) - 88usize];
};
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_vad_segment {
    pub start_ms: i64,
    pub end_ms: i64,
    pub confidence: f32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_vad_segment"][::std::mem::size_of::<transcribe_vad_segment>() - 24usize];
    ["Alignment of transcribe_vad_segment"]
        [::std::mem::align_of::<transcribe_vad_segment>() - 8usize];
    ["Offset of field: transcribe_vad_segment::start_ms"]
        [::std::mem::offset_of!(transcribe_vad_segment, start_ms) - 0usize];
    ["Offset of field: transcribe_vad_segment::end_ms"]
        [::std::mem::offset_of!(transcribe_vad_segment, end_ms) - 8usize];
    ["Offset of field: transcribe_vad_segment::confidence"]
        [::std::mem::offset_of!(transcribe_vad_segment, confidence) - 16usize];
};


#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_run_params {
    pub struct_size: u64,
    pub task: transcribe_task,
    pub timestamps: transcribe_timestamp_kind,
    pub pnc: transcribe_pnc_mode,
    pub itn: transcribe_itn_mode,
    pub diarize: transcribe_diarize_mode,
    pub language: *const ::std::os::raw::c_char,
    pub target_language: *const ::std::os::raw::c_char,
    pub keep_special_tags: bool,
    pub family: *const transcribe_ext,
    pub spec_k_drafts: i32,
    pub vad: transcribe_vad_params,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_run_params"][::std::mem::size_of::<transcribe_run_params>() - 168usize];
    ["Alignment of transcribe_run_params"]
        [::std::mem::align_of::<transcribe_run_params>() - 8usize];
    ["Offset of field: transcribe_run_params::struct_size"]
        [::std::mem::offset_of!(transcribe_run_params, struct_size) - 0usize];
    ["Offset of field: transcribe_run_params::task"]
        [::std::mem::offset_of!(transcribe_run_params, task) - 8usize];
    ["Offset of field: transcribe_run_params::timestamps"]
        [::std::mem::offset_of!(transcribe_run_params, timestamps) - 12usize];
    ["Offset of field: transcribe_run_params::pnc"]
        [::std::mem::offset_of!(transcribe_run_params, pnc) - 16usize];
    ["Offset of field: transcribe_run_params::itn"]
        [::std::mem::offset_of!(transcribe_run_params, itn) - 20usize];
    ["Offset of field: transcribe_run_params::diarize"]
        [::std::mem::offset_of!(transcribe_run_params, diarize) - 24usize];
    ["Offset of field: transcribe_run_params::language"]
        [::std::mem::offset_of!(transcribe_run_params, language) - 32usize];
    ["Offset of field: transcribe_run_params::target_language"]
        [::std::mem::offset_of!(transcribe_run_params, target_language) - 40usize];
    ["Offset of field: transcribe_run_params::keep_special_tags"]
        [::std::mem::offset_of!(transcribe_run_params, keep_special_tags) - 48usize];
    ["Offset of field: transcribe_run_params::family"]
        [::std::mem::offset_of!(transcribe_run_params, family) - 56usize];
    ["Offset of field: transcribe_run_params::spec_k_drafts"]
        [::std::mem::offset_of!(transcribe_run_params, spec_k_drafts) - 64usize];
    ["Offset of field: transcribe_run_params::vad"]
        [::std::mem::offset_of!(transcribe_run_params, vad) - 72usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_capabilities {
    pub struct_size: u64,
    pub native_sample_rate: i32,
    pub n_languages: ::std::os::raw::c_int,
    pub languages: *const *const ::std::os::raw::c_char,
    pub max_timestamp_kind: transcribe_timestamp_kind,
    pub supports_language_detect: bool,
    pub supports_translate: bool,
    pub supports_streaming: bool,
    pub supports_spec_decode: bool,
    pub max_audio_ms: i64,
    pub n_translate_target_languages: ::std::os::raw::c_int,
    pub translate_target_languages: *const *const ::std::os::raw::c_char,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_capabilities"][::std::mem::size_of::<transcribe_capabilities>() - 56usize];
    ["Alignment of transcribe_capabilities"]
        [::std::mem::align_of::<transcribe_capabilities>() - 8usize];
    ["Offset of field: transcribe_capabilities::struct_size"]
        [::std::mem::offset_of!(transcribe_capabilities, struct_size) - 0usize];
    ["Offset of field: transcribe_capabilities::native_sample_rate"]
        [::std::mem::offset_of!(transcribe_capabilities, native_sample_rate) - 8usize];
    ["Offset of field: transcribe_capabilities::n_languages"]
        [::std::mem::offset_of!(transcribe_capabilities, n_languages) - 12usize];
    ["Offset of field: transcribe_capabilities::languages"]
        [::std::mem::offset_of!(transcribe_capabilities, languages) - 16usize];
    ["Offset of field: transcribe_capabilities::max_timestamp_kind"]
        [::std::mem::offset_of!(transcribe_capabilities, max_timestamp_kind) - 24usize];
    ["Offset of field: transcribe_capabilities::supports_language_detect"]
        [::std::mem::offset_of!(transcribe_capabilities, supports_language_detect) - 28usize];
    ["Offset of field: transcribe_capabilities::supports_translate"]
        [::std::mem::offset_of!(transcribe_capabilities, supports_translate) - 29usize];
    ["Offset of field: transcribe_capabilities::supports_streaming"]
        [::std::mem::offset_of!(transcribe_capabilities, supports_streaming) - 30usize];
    ["Offset of field: transcribe_capabilities::supports_spec_decode"]
        [::std::mem::offset_of!(transcribe_capabilities, supports_spec_decode) - 31usize];
    ["Offset of field: transcribe_capabilities::max_audio_ms"]
        [::std::mem::offset_of!(transcribe_capabilities, max_audio_ms) - 32usize];
    ["Offset of field: transcribe_capabilities::n_translate_target_languages"]
        [::std::mem::offset_of!(transcribe_capabilities, n_translate_target_languages) - 40usize];
    ["Offset of field: transcribe_capabilities::translate_target_languages"]
        [::std::mem::offset_of!(transcribe_capabilities, translate_target_languages) - 48usize];
};


impl transcribe_feature {
    pub const TRANSCRIBE_FEATURE_INITIAL_PROMPT: transcribe_feature = transcribe_feature(0);
    pub const TRANSCRIBE_FEATURE_TEMPERATURE_FALLBACK: transcribe_feature = transcribe_feature(1);
    pub const TRANSCRIBE_FEATURE_LONG_FORM: transcribe_feature = transcribe_feature(2);
    pub const TRANSCRIBE_FEATURE_CANCELLATION: transcribe_feature = transcribe_feature(3);
    pub const TRANSCRIBE_FEATURE_PNC: transcribe_feature = transcribe_feature(4);
    pub const TRANSCRIBE_FEATURE_ITN: transcribe_feature = transcribe_feature(5);
    pub const TRANSCRIBE_FEATURE_DIARIZATION: transcribe_feature = transcribe_feature(6);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_feature(pub ::std::os::raw::c_uint);














pub type transcribe_abort_callback =
    ::std::option::Option<unsafe extern "C" fn(user_data: *mut ::std::os::raw::c_void) -> bool>;

pub type transcribe_progress_callback = ::std::option::Option<
    unsafe extern "C" fn(
        progress: f32,
        stage: *const ::std::os::raw::c_char,
        completed_units: i64,
        total_units: i64,
        user_data: *mut ::std::os::raw::c_void,
    ) -> ::std::os::raw::c_int,
>;



#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_session_limits {
    pub struct_size: u64,
    pub effective_n_ctx: i32,
    pub effective_max_audio_ms: i64,
    pub max_kv_bytes: i64,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_session_limits"]
        [::std::mem::size_of::<transcribe_session_limits>() - 32usize];
    ["Alignment of transcribe_session_limits"]
        [::std::mem::align_of::<transcribe_session_limits>() - 8usize];
    ["Offset of field: transcribe_session_limits::struct_size"]
        [::std::mem::offset_of!(transcribe_session_limits, struct_size) - 0usize];
    ["Offset of field: transcribe_session_limits::effective_n_ctx"]
        [::std::mem::offset_of!(transcribe_session_limits, effective_n_ctx) - 8usize];
    ["Offset of field: transcribe_session_limits::effective_max_audio_ms"]
        [::std::mem::offset_of!(transcribe_session_limits, effective_max_audio_ms) - 16usize];
    ["Offset of field: transcribe_session_limits::max_kv_bytes"]
        [::std::mem::offset_of!(transcribe_session_limits, max_kv_bytes) - 24usize];
};


impl transcribe_stream_state {
    pub const TRANSCRIBE_STREAM_IDLE: transcribe_stream_state = transcribe_stream_state(0);
    pub const TRANSCRIBE_STREAM_ACTIVE: transcribe_stream_state = transcribe_stream_state(1);
    pub const TRANSCRIBE_STREAM_FINISHED: transcribe_stream_state = transcribe_stream_state(2);
    pub const TRANSCRIBE_STREAM_FAILED: transcribe_stream_state = transcribe_stream_state(3);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_stream_state(pub ::std::os::raw::c_uint);
impl transcribe_stream_commit_policy {
    pub const TRANSCRIBE_STREAM_COMMIT_AUTO: transcribe_stream_commit_policy =
        transcribe_stream_commit_policy(0);
    pub const TRANSCRIBE_STREAM_COMMIT_ON_FINALIZE: transcribe_stream_commit_policy =
        transcribe_stream_commit_policy(1);
    pub const TRANSCRIBE_STREAM_COMMIT_STABLE_PREFIX: transcribe_stream_commit_policy =
        transcribe_stream_commit_policy(2);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_stream_commit_policy(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_stream_params {
    pub struct_size: u64,
    pub family: *const transcribe_ext,
    pub commit_policy: transcribe_stream_commit_policy,
    pub stable_prefix_agreement_n: u32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_stream_params"]
        [::std::mem::size_of::<transcribe_stream_params>() - 24usize];
    ["Alignment of transcribe_stream_params"]
        [::std::mem::align_of::<transcribe_stream_params>() - 8usize];
    ["Offset of field: transcribe_stream_params::struct_size"]
        [::std::mem::offset_of!(transcribe_stream_params, struct_size) - 0usize];
    ["Offset of field: transcribe_stream_params::family"]
        [::std::mem::offset_of!(transcribe_stream_params, family) - 8usize];
    ["Offset of field: transcribe_stream_params::commit_policy"]
        [::std::mem::offset_of!(transcribe_stream_params, commit_policy) - 16usize];
    ["Offset of field: transcribe_stream_params::stable_prefix_agreement_n"]
        [::std::mem::offset_of!(transcribe_stream_params, stable_prefix_agreement_n) - 20usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_stream_update {
    pub struct_size: u64,
    pub result_changed: bool,
    pub is_final: bool,
    pub revision: i32,
    pub input_received_ms: i64,
    pub audio_committed_ms: i64,
    pub buffered_ms: i64,
    pub committed_changed: bool,
    pub tentative_changed: bool,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_stream_update"]
        [::std::mem::size_of::<transcribe_stream_update>() - 48usize];
    ["Alignment of transcribe_stream_update"]
        [::std::mem::align_of::<transcribe_stream_update>() - 8usize];
    ["Offset of field: transcribe_stream_update::struct_size"]
        [::std::mem::offset_of!(transcribe_stream_update, struct_size) - 0usize];
    ["Offset of field: transcribe_stream_update::result_changed"]
        [::std::mem::offset_of!(transcribe_stream_update, result_changed) - 8usize];
    ["Offset of field: transcribe_stream_update::is_final"]
        [::std::mem::offset_of!(transcribe_stream_update, is_final) - 9usize];
    ["Offset of field: transcribe_stream_update::revision"]
        [::std::mem::offset_of!(transcribe_stream_update, revision) - 12usize];
    ["Offset of field: transcribe_stream_update::input_received_ms"]
        [::std::mem::offset_of!(transcribe_stream_update, input_received_ms) - 16usize];
    ["Offset of field: transcribe_stream_update::audio_committed_ms"]
        [::std::mem::offset_of!(transcribe_stream_update, audio_committed_ms) - 24usize];
    ["Offset of field: transcribe_stream_update::buffered_ms"]
        [::std::mem::offset_of!(transcribe_stream_update, buffered_ms) - 32usize];
    ["Offset of field: transcribe_stream_update::committed_changed"]
        [::std::mem::offset_of!(transcribe_stream_update, committed_changed) - 40usize];
    ["Offset of field: transcribe_stream_update::tentative_changed"]
        [::std::mem::offset_of!(transcribe_stream_update, tentative_changed) - 41usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_stream_text {
    pub struct_size: u64,
    pub full_text: *const ::std::os::raw::c_char,
    pub full_text_bytes: u64,
    pub committed_text: *const ::std::os::raw::c_char,
    pub committed_text_bytes: u64,
    pub tentative_text: *const ::std::os::raw::c_char,
    pub tentative_text_bytes: u64,
    pub raw_tentative_start_bytes: u64,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_stream_text"][::std::mem::size_of::<transcribe_stream_text>() - 64usize];
    ["Alignment of transcribe_stream_text"]
        [::std::mem::align_of::<transcribe_stream_text>() - 8usize];
    ["Offset of field: transcribe_stream_text::struct_size"]
        [::std::mem::offset_of!(transcribe_stream_text, struct_size) - 0usize];
    ["Offset of field: transcribe_stream_text::full_text"]
        [::std::mem::offset_of!(transcribe_stream_text, full_text) - 8usize];
    ["Offset of field: transcribe_stream_text::full_text_bytes"]
        [::std::mem::offset_of!(transcribe_stream_text, full_text_bytes) - 16usize];
    ["Offset of field: transcribe_stream_text::committed_text"]
        [::std::mem::offset_of!(transcribe_stream_text, committed_text) - 24usize];
    ["Offset of field: transcribe_stream_text::committed_text_bytes"]
        [::std::mem::offset_of!(transcribe_stream_text, committed_text_bytes) - 32usize];
    ["Offset of field: transcribe_stream_text::tentative_text"]
        [::std::mem::offset_of!(transcribe_stream_text, tentative_text) - 40usize];
    ["Offset of field: transcribe_stream_text::tentative_text_bytes"]
        [::std::mem::offset_of!(transcribe_stream_text, tentative_text_bytes) - 48usize];
    ["Offset of field: transcribe_stream_text::raw_tentative_start_bytes"]
        [::std::mem::offset_of!(transcribe_stream_text, raw_tentative_start_bytes) - 56usize];
};













#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_timings {
    pub struct_size: u64,
    pub load_ms: f32,
    pub mel_ms: f32,
    pub encode_ms: f32,
    pub decode_ms: f32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_timings"][::std::mem::size_of::<transcribe_timings>() - 24usize];
    ["Alignment of transcribe_timings"][::std::mem::align_of::<transcribe_timings>() - 8usize];
    ["Offset of field: transcribe_timings::struct_size"]
        [::std::mem::offset_of!(transcribe_timings, struct_size) - 0usize];
    ["Offset of field: transcribe_timings::load_ms"]
        [::std::mem::offset_of!(transcribe_timings, load_ms) - 8usize];
    ["Offset of field: transcribe_timings::mel_ms"]
        [::std::mem::offset_of!(transcribe_timings, mel_ms) - 12usize];
    ["Offset of field: transcribe_timings::encode_ms"]
        [::std::mem::offset_of!(transcribe_timings, encode_ms) - 16usize];
    ["Offset of field: transcribe_timings::decode_ms"]
        [::std::mem::offset_of!(transcribe_timings, decode_ms) - 20usize];
};











#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_segment {
    pub struct_size: u64,
    pub t0_ms: i64,
    pub t1_ms: i64,
    pub first_word: ::std::os::raw::c_int,
    pub n_words: ::std::os::raw::c_int,
    pub first_token: ::std::os::raw::c_int,
    pub n_tokens: ::std::os::raw::c_int,
    pub text: *const ::std::os::raw::c_char,
    pub speaker_id: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_segment"][::std::mem::size_of::<transcribe_segment>() - 56usize];
    ["Alignment of transcribe_segment"][::std::mem::align_of::<transcribe_segment>() - 8usize];
    ["Offset of field: transcribe_segment::struct_size"]
        [::std::mem::offset_of!(transcribe_segment, struct_size) - 0usize];
    ["Offset of field: transcribe_segment::t0_ms"]
        [::std::mem::offset_of!(transcribe_segment, t0_ms) - 8usize];
    ["Offset of field: transcribe_segment::t1_ms"]
        [::std::mem::offset_of!(transcribe_segment, t1_ms) - 16usize];
    ["Offset of field: transcribe_segment::first_word"]
        [::std::mem::offset_of!(transcribe_segment, first_word) - 24usize];
    ["Offset of field: transcribe_segment::n_words"]
        [::std::mem::offset_of!(transcribe_segment, n_words) - 28usize];
    ["Offset of field: transcribe_segment::first_token"]
        [::std::mem::offset_of!(transcribe_segment, first_token) - 32usize];
    ["Offset of field: transcribe_segment::n_tokens"]
        [::std::mem::offset_of!(transcribe_segment, n_tokens) - 36usize];
    ["Offset of field: transcribe_segment::text"]
        [::std::mem::offset_of!(transcribe_segment, text) - 40usize];
    ["Offset of field: transcribe_segment::speaker_id"]
        [::std::mem::offset_of!(transcribe_segment, speaker_id) - 48usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_word {
    pub struct_size: u64,
    pub t0_ms: i64,
    pub t1_ms: i64,
    pub seg_index: ::std::os::raw::c_int,
    pub first_token: ::std::os::raw::c_int,
    pub n_tokens: ::std::os::raw::c_int,
    pub text: *const ::std::os::raw::c_char,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_word"][::std::mem::size_of::<transcribe_word>() - 48usize];
    ["Alignment of transcribe_word"][::std::mem::align_of::<transcribe_word>() - 8usize];
    ["Offset of field: transcribe_word::struct_size"]
        [::std::mem::offset_of!(transcribe_word, struct_size) - 0usize];
    ["Offset of field: transcribe_word::t0_ms"]
        [::std::mem::offset_of!(transcribe_word, t0_ms) - 8usize];
    ["Offset of field: transcribe_word::t1_ms"]
        [::std::mem::offset_of!(transcribe_word, t1_ms) - 16usize];
    ["Offset of field: transcribe_word::seg_index"]
        [::std::mem::offset_of!(transcribe_word, seg_index) - 24usize];
    ["Offset of field: transcribe_word::first_token"]
        [::std::mem::offset_of!(transcribe_word, first_token) - 28usize];
    ["Offset of field: transcribe_word::n_tokens"]
        [::std::mem::offset_of!(transcribe_word, n_tokens) - 32usize];
    ["Offset of field: transcribe_word::text"]
        [::std::mem::offset_of!(transcribe_word, text) - 40usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_token {
    pub struct_size: u64,
    pub id: ::std::os::raw::c_int,
    pub p: f32,
    pub t0_ms: i64,
    pub t1_ms: i64,
    pub seg_index: ::std::os::raw::c_int,
    pub word_index: ::std::os::raw::c_int,
    pub text: *const ::std::os::raw::c_char,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_token"][::std::mem::size_of::<transcribe_token>() - 48usize];
    ["Alignment of transcribe_token"][::std::mem::align_of::<transcribe_token>() - 8usize];
    ["Offset of field: transcribe_token::struct_size"]
        [::std::mem::offset_of!(transcribe_token, struct_size) - 0usize];
    ["Offset of field: transcribe_token::id"]
        [::std::mem::offset_of!(transcribe_token, id) - 8usize];
    ["Offset of field: transcribe_token::p"][::std::mem::offset_of!(transcribe_token, p) - 12usize];
    ["Offset of field: transcribe_token::t0_ms"]
        [::std::mem::offset_of!(transcribe_token, t0_ms) - 16usize];
    ["Offset of field: transcribe_token::t1_ms"]
        [::std::mem::offset_of!(transcribe_token, t1_ms) - 24usize];
    ["Offset of field: transcribe_token::seg_index"]
        [::std::mem::offset_of!(transcribe_token, seg_index) - 32usize];
    ["Offset of field: transcribe_token::word_index"]
        [::std::mem::offset_of!(transcribe_token, word_index) - 36usize];
    ["Offset of field: transcribe_token::text"]
        [::std::mem::offset_of!(transcribe_token, text) - 40usize];
};




#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_speaker_segment {
    pub struct_size: u64,
    pub t0_ms: i64,
    pub t1_ms: i64,
    pub speaker_id: i32,
    pub p: f32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_speaker_segment"]
        [::std::mem::size_of::<transcribe_speaker_segment>() - 32usize];
    ["Alignment of transcribe_speaker_segment"]
        [::std::mem::align_of::<transcribe_speaker_segment>() - 8usize];
    ["Offset of field: transcribe_speaker_segment::struct_size"]
        [::std::mem::offset_of!(transcribe_speaker_segment, struct_size) - 0usize];
    ["Offset of field: transcribe_speaker_segment::t0_ms"]
        [::std::mem::offset_of!(transcribe_speaker_segment, t0_ms) - 8usize];
    ["Offset of field: transcribe_speaker_segment::t1_ms"]
        [::std::mem::offset_of!(transcribe_speaker_segment, t1_ms) - 16usize];
    ["Offset of field: transcribe_speaker_segment::speaker_id"]
        [::std::mem::offset_of!(transcribe_speaker_segment, speaker_id) - 24usize];
    ["Offset of field: transcribe_speaker_segment::p"]
        [::std::mem::offset_of!(transcribe_speaker_segment, p) - 28usize];
};


















#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_moonshine_streaming_stream_ext {
    pub ext: transcribe_ext,
    pub min_decode_interval_ms: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_moonshine_streaming_stream_ext"]
        [::std::mem::size_of::<transcribe_moonshine_streaming_stream_ext>() - 24usize];
    ["Alignment of transcribe_moonshine_streaming_stream_ext"]
        [::std::mem::align_of::<transcribe_moonshine_streaming_stream_ext>() - 8usize];
    ["Offset of field: transcribe_moonshine_streaming_stream_ext::ext"]
        [::std::mem::offset_of!(transcribe_moonshine_streaming_stream_ext, ext) - 0usize];
    ["Offset of field: transcribe_moonshine_streaming_stream_ext::min_decode_interval_ms"][::std::mem::offset_of!(
        transcribe_moonshine_streaming_stream_ext,
        min_decode_interval_ms
    )
        - 16usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_parakeet_stream_ext {
    pub ext: transcribe_ext,
    pub att_context_right: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_parakeet_stream_ext"]
        [::std::mem::size_of::<transcribe_parakeet_stream_ext>() - 24usize];
    ["Alignment of transcribe_parakeet_stream_ext"]
        [::std::mem::align_of::<transcribe_parakeet_stream_ext>() - 8usize];
    ["Offset of field: transcribe_parakeet_stream_ext::ext"]
        [::std::mem::offset_of!(transcribe_parakeet_stream_ext, ext) - 0usize];
    ["Offset of field: transcribe_parakeet_stream_ext::att_context_right"]
        [::std::mem::offset_of!(transcribe_parakeet_stream_ext, att_context_right) - 16usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_parakeet_buffered_stream_ext {
    pub ext: transcribe_ext,
    pub left_ms: i32,
    pub chunk_ms: i32,
    pub right_ms: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_parakeet_buffered_stream_ext"]
        [::std::mem::size_of::<transcribe_parakeet_buffered_stream_ext>() - 32usize];
    ["Alignment of transcribe_parakeet_buffered_stream_ext"]
        [::std::mem::align_of::<transcribe_parakeet_buffered_stream_ext>() - 8usize];
    ["Offset of field: transcribe_parakeet_buffered_stream_ext::ext"]
        [::std::mem::offset_of!(transcribe_parakeet_buffered_stream_ext, ext) - 0usize];
    ["Offset of field: transcribe_parakeet_buffered_stream_ext::left_ms"]
        [::std::mem::offset_of!(transcribe_parakeet_buffered_stream_ext, left_ms) - 16usize];
    ["Offset of field: transcribe_parakeet_buffered_stream_ext::chunk_ms"]
        [::std::mem::offset_of!(transcribe_parakeet_buffered_stream_ext, chunk_ms) - 20usize];
    ["Offset of field: transcribe_parakeet_buffered_stream_ext::right_ms"]
        [::std::mem::offset_of!(transcribe_parakeet_buffered_stream_ext, right_ms) - 24usize];
};

impl transcribe_sortformer_preset {
    pub const TRANSCRIBE_SORTFORMER_PRESET_DEFAULT: transcribe_sortformer_preset =
        transcribe_sortformer_preset(0);
    pub const TRANSCRIBE_SORTFORMER_PRESET_VERY_HIGH_LATENCY: transcribe_sortformer_preset =
        transcribe_sortformer_preset(1);
    pub const TRANSCRIBE_SORTFORMER_PRESET_HIGH_LATENCY: transcribe_sortformer_preset =
        transcribe_sortformer_preset(2);
    pub const TRANSCRIBE_SORTFORMER_PRESET_LOW_LATENCY: transcribe_sortformer_preset =
        transcribe_sortformer_preset(3);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_sortformer_preset(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_sortformer_stream_ext {
    pub ext: transcribe_ext,
    pub preset: transcribe_sortformer_preset,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_sortformer_stream_ext"]
        [::std::mem::size_of::<transcribe_sortformer_stream_ext>() - 24usize];
    ["Alignment of transcribe_sortformer_stream_ext"]
        [::std::mem::align_of::<transcribe_sortformer_stream_ext>() - 8usize];
    ["Offset of field: transcribe_sortformer_stream_ext::ext"]
        [::std::mem::offset_of!(transcribe_sortformer_stream_ext, ext) - 0usize];
    ["Offset of field: transcribe_sortformer_stream_ext::preset"]
        [::std::mem::offset_of!(transcribe_sortformer_stream_ext, preset) - 16usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_voxtral_realtime_stream_ext {
    pub ext: transcribe_ext,
    pub num_delay_tokens: i32,
    pub min_decode_interval_ms: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_voxtral_realtime_stream_ext"]
        [::std::mem::size_of::<transcribe_voxtral_realtime_stream_ext>() - 24usize];
    ["Alignment of transcribe_voxtral_realtime_stream_ext"]
        [::std::mem::align_of::<transcribe_voxtral_realtime_stream_ext>() - 8usize];
    ["Offset of field: transcribe_voxtral_realtime_stream_ext::ext"]
        [::std::mem::offset_of!(transcribe_voxtral_realtime_stream_ext, ext) - 0usize];
    ["Offset of field: transcribe_voxtral_realtime_stream_ext::num_delay_tokens"][::std::mem::offset_of!(
        transcribe_voxtral_realtime_stream_ext,
        num_delay_tokens
    ) - 16usize];
    ["Offset of field: transcribe_voxtral_realtime_stream_ext::min_decode_interval_ms"][::std::mem::offset_of!(
        transcribe_voxtral_realtime_stream_ext,
        min_decode_interval_ms
    )
        - 20usize];
};

impl transcribe_whisper_prompt_condition {
    pub const TRANSCRIBE_WHISPER_PROMPT_FIRST_SEGMENT: transcribe_whisper_prompt_condition =
        transcribe_whisper_prompt_condition(0);
    pub const TRANSCRIBE_WHISPER_PROMPT_ALL_SEGMENTS: transcribe_whisper_prompt_condition =
        transcribe_whisper_prompt_condition(1);
}
#[repr(transparent)]
#[derive(Debug, Copy, Clone, Hash, PartialEq, Eq)]
pub struct transcribe_whisper_prompt_condition(pub ::std::os::raw::c_uint);
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_whisper_run_ext {
    pub ext: transcribe_ext,
    pub initial_prompt: *const ::std::os::raw::c_char,
    pub prompt_tokens: *const i32,
    pub n_prompt_tokens: usize,
    pub prompt_condition: transcribe_whisper_prompt_condition,
    pub condition_on_prev_tokens: bool,
    pub max_prev_context_tokens: i32,
    pub temperature: f32,
    pub temperature_inc: f32,
    pub compression_ratio_thold: f32,
    pub logprob_thold: f32,
    pub no_speech_thold: f32,
    pub seed: u32,
    pub max_initial_timestamp: f32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_whisper_run_ext"]
        [::std::mem::size_of::<transcribe_whisper_run_ext>() - 80usize];
    ["Alignment of transcribe_whisper_run_ext"]
        [::std::mem::align_of::<transcribe_whisper_run_ext>() - 8usize];
    ["Offset of field: transcribe_whisper_run_ext::ext"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, ext) - 0usize];
    ["Offset of field: transcribe_whisper_run_ext::initial_prompt"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, initial_prompt) - 16usize];
    ["Offset of field: transcribe_whisper_run_ext::prompt_tokens"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, prompt_tokens) - 24usize];
    ["Offset of field: transcribe_whisper_run_ext::n_prompt_tokens"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, n_prompt_tokens) - 32usize];
    ["Offset of field: transcribe_whisper_run_ext::prompt_condition"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, prompt_condition) - 40usize];
    ["Offset of field: transcribe_whisper_run_ext::condition_on_prev_tokens"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, condition_on_prev_tokens) - 44usize];
    ["Offset of field: transcribe_whisper_run_ext::max_prev_context_tokens"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, max_prev_context_tokens) - 48usize];
    ["Offset of field: transcribe_whisper_run_ext::temperature"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, temperature) - 52usize];
    ["Offset of field: transcribe_whisper_run_ext::temperature_inc"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, temperature_inc) - 56usize];
    ["Offset of field: transcribe_whisper_run_ext::compression_ratio_thold"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, compression_ratio_thold) - 60usize];
    ["Offset of field: transcribe_whisper_run_ext::logprob_thold"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, logprob_thold) - 64usize];
    ["Offset of field: transcribe_whisper_run_ext::no_speech_thold"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, no_speech_thold) - 68usize];
    ["Offset of field: transcribe_whisper_run_ext::seed"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, seed) - 72usize];
    ["Offset of field: transcribe_whisper_run_ext::max_initial_timestamp"]
        [::std::mem::offset_of!(transcribe_whisper_run_ext, max_initial_timestamp) - 76usize];
};

#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct transcribe_whisper_chunk_trace {
    pub struct_size: u64,
    pub t0_ms: i64,
    pub t1_ms: i64,
    pub temperature_used: f32,
    pub compression_ratio: f32,
    pub avg_logprob: f32,
    pub no_speech_prob: f32,
    pub no_speech_triggered: bool,
    pub n_fallbacks: i32,
}
#[allow(clippy::unnecessary_operation, clippy::identity_op)]
const _: () = {
    ["Size of transcribe_whisper_chunk_trace"]
        [::std::mem::size_of::<transcribe_whisper_chunk_trace>() - 48usize];
    ["Alignment of transcribe_whisper_chunk_trace"]
        [::std::mem::align_of::<transcribe_whisper_chunk_trace>() - 8usize];
    ["Offset of field: transcribe_whisper_chunk_trace::struct_size"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, struct_size) - 0usize];
    ["Offset of field: transcribe_whisper_chunk_trace::t0_ms"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, t0_ms) - 8usize];
    ["Offset of field: transcribe_whisper_chunk_trace::t1_ms"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, t1_ms) - 16usize];
    ["Offset of field: transcribe_whisper_chunk_trace::temperature_used"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, temperature_used) - 24usize];
    ["Offset of field: transcribe_whisper_chunk_trace::compression_ratio"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, compression_ratio) - 28usize];
    ["Offset of field: transcribe_whisper_chunk_trace::avg_logprob"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, avg_logprob) - 32usize];
    ["Offset of field: transcribe_whisper_chunk_trace::no_speech_prob"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, no_speech_prob) - 36usize];
    ["Offset of field: transcribe_whisper_chunk_trace::no_speech_triggered"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, no_speech_triggered) - 40usize];
    ["Offset of field: transcribe_whisper_chunk_trace::n_fallbacks"]
        [::std::mem::offset_of!(transcribe_whisper_chunk_trace, n_fallbacks) - 44usize];
};




// ─── dynload trampolines (feature `dynload`) ───────────────────────────
// Each C entry point is shadowed by a same-named Rust trampoline that
// forwards through the runtime-loaded library (crate::dynload). The
// index in TRANSCRIBE_DYN_SYMBOLS is the trampoline index: the loader
// resolves every name at init and fails fast on any missing symbol.
// When the library is not loaded a type-shaped default is returned
// (null / an error status) instead of calling through a null pointer.

pub(crate) const TRANSCRIBE_DYN_SYMBOLS: &[&[u8]] = &[
    b"transcribe_status_string",
    b"transcribe_version",
    b"transcribe_version_commit",
    b"transcribe_abi_struct_size",
    b"transcribe_abi_struct_align",
    b"transcribe_log_set",
    b"transcribe_ext_check",
    b"transcribe_model_accepts_ext_kind",
    b"transcribe_init_backends",
    b"transcribe_init_backends_default",
    b"transcribe_backend_device_count",
    b"transcribe_backend_device_init",
    b"transcribe_get_backend_device",
    b"transcribe_backend_available",
    b"transcribe_model_get_device",
    b"transcribe_model_load_params_init",
    b"transcribe_session_params_init",
    b"transcribe_vad",
    b"transcribe_free_vad",
    b"transcribe_run_params_init",
    b"transcribe_capabilities_init",
    b"transcribe_model_get_capabilities",
    b"transcribe_model_supports",
    b"transcribe_model_arch_string",
    b"transcribe_model_variant_string",
    b"transcribe_model_backend",
    b"transcribe_model_meta_val_str",
    b"transcribe_model_load_file",
    b"transcribe_model_free",
    b"transcribe_session_init",
    b"transcribe_session_free",
    b"transcribe_open",
    b"transcribe_close",
    b"transcribe_get_model",
    b"transcribe_run",
    b"transcribe_run_batch",
    b"transcribe_set_abort_callback",
    b"transcribe_set_progress_callback",
    b"transcribe_was_aborted",
    b"transcribe_was_truncated",
    b"transcribe_session_limits_init",
    b"transcribe_session_get_limits",
    b"transcribe_stream_params_init",
    b"transcribe_stream_update_init",
    b"transcribe_stream_text_init",
    b"transcribe_stream_get_text",
    b"transcribe_stream_begin",
    b"transcribe_stream_feed",
    b"transcribe_stream_finalize",
    b"transcribe_stream_reset",
    b"transcribe_stream_get_state",
    b"transcribe_stream_revision",
    b"transcribe_stream_n_committed_segments",
    b"transcribe_stream_n_committed_words",
    b"transcribe_stream_n_committed_tokens",
    b"transcribe_stream_last_status",
    b"transcribe_tokenize",
    b"transcribe_timings_init",
    b"transcribe_get_timings",
    b"transcribe_print_timings",
    b"transcribe_reset_timings",
    b"transcribe_full_text",
    b"transcribe_raw_text",
    b"transcribe_returned_timestamp_kind",
    b"transcribe_n_segments",
    b"transcribe_n_words",
    b"transcribe_n_tokens",
    b"transcribe_detected_language",
    b"transcribe_segment_init",
    b"transcribe_word_init",
    b"transcribe_token_init",
    b"transcribe_get_segment",
    b"transcribe_get_word",
    b"transcribe_get_token",
    b"transcribe_speaker_segment_init",
    b"transcribe_n_speaker_segments",
    b"transcribe_get_speaker_segment",
    b"transcribe_batch_n_results",
    b"transcribe_batch_status",
    b"transcribe_batch_full_text",
    b"transcribe_batch_raw_text",
    b"transcribe_batch_returned_timestamp_kind",
    b"transcribe_batch_detected_language",
    b"transcribe_batch_n_segments",
    b"transcribe_batch_n_words",
    b"transcribe_batch_n_tokens",
    b"transcribe_batch_get_segment",
    b"transcribe_batch_get_word",
    b"transcribe_batch_get_token",
    b"transcribe_batch_n_speaker_segments",
    b"transcribe_batch_get_speaker_segment",
    b"transcribe_batch_get_timings",
    b"transcribe_moonshine_streaming_stream_ext_init",
    b"transcribe_parakeet_stream_ext_init",
    b"transcribe_parakeet_buffered_stream_ext_init",
    b"transcribe_sortformer_stream_ext_init",
    b"transcribe_voxtral_realtime_stream_ext_init",
    b"transcribe_whisper_run_ext_init",
    b"transcribe_whisper_chunk_trace_init",
    b"transcribe_get_whisper_chunk_count",
    b"transcribe_get_whisper_chunk_trace",
];

/// Runtime trampoline for `transcribe_status_string` — index 0 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_status_string(status: ::std::os::raw::c_int) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(0) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(::std::os::raw::c_int) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(status) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_version` — index 1 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_version() -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(1) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn() -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f() }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_version_commit` — index 2 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_version_commit() -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(2) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn() -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f() }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_abi_struct_size` — index 3 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_abi_struct_size(which: transcribe_abi_struct) -> usize {
    match crate::dynload::fn_ptr(3) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(transcribe_abi_struct) -> usize = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(which) }
        }
        None => 0,
    }
}

/// Runtime trampoline for `transcribe_abi_struct_align` — index 4 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_abi_struct_align(which: transcribe_abi_struct) -> usize {
    match crate::dynload::fn_ptr(4) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(transcribe_abi_struct) -> usize = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(which) }
        }
        None => 0,
    }
}

/// Runtime trampoline for `transcribe_log_set` — index 5 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_log_set(cb: transcribe_log_callback, userdata: *mut ::std::os::raw::c_void) -> () {
    match crate::dynload::fn_ptr(5) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(transcribe_log_callback, *mut ::std::os::raw::c_void) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(cb, userdata) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_ext_check` — index 6 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_ext_check(ext: *const transcribe_ext, expected_kind: u32, min_size: u64) -> transcribe_status {
    match crate::dynload::fn_ptr(6) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_ext, u32, u64) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext, expected_kind, min_size) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_model_accepts_ext_kind` — index 7 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_accepts_ext_kind(model: *const transcribe_model, slot: transcribe_ext_slot, kind: u32) -> bool {
    match crate::dynload::fn_ptr(7) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, transcribe_ext_slot, u32) -> bool = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, slot, kind) }
        }
        None => false,
    }
}

/// Runtime trampoline for `transcribe_init_backends` — index 8 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_init_backends(artifact_dir: *const ::std::os::raw::c_char) -> transcribe_status {
    match crate::dynload::fn_ptr(8) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const ::std::os::raw::c_char) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(artifact_dir) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_init_backends_default` — index 9 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_init_backends_default() -> transcribe_status {
    match crate::dynload::fn_ptr(9) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn() -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f() }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_backend_device_count` — index 10 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_backend_device_count() -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(10) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn() -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f() }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_backend_device_init` — index 11 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_backend_device_init(p: *mut transcribe_backend_device) -> () {
    match crate::dynload::fn_ptr(11) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_backend_device) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(p) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_get_backend_device` — index 12 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_backend_device(index: ::std::os::raw::c_int, out: *mut transcribe_backend_device) -> transcribe_status {
    match crate::dynload::fn_ptr(12) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(::std::os::raw::c_int, *mut transcribe_backend_device) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(index, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_backend_available` — index 13 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_backend_available(kind: transcribe_backend_request) -> bool {
    match crate::dynload::fn_ptr(13) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(transcribe_backend_request) -> bool = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(kind) }
        }
        None => false,
    }
}

/// Runtime trampoline for `transcribe_model_get_device` — index 14 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_get_device(model: *const transcribe_model, out: *mut transcribe_backend_device) -> transcribe_status {
    match crate::dynload::fn_ptr(14) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, *mut transcribe_backend_device) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_model_load_params_init` — index 15 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_load_params_init(params: *mut transcribe_model_load_params) -> () {
    match crate::dynload::fn_ptr(15) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_model_load_params) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(params) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_session_params_init` — index 16 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_session_params_init(params: *mut transcribe_session_params) -> () {
    match crate::dynload::fn_ptr(16) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session_params) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(params) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_vad` — index 17 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_vad(pcm: *const f32, n_samples: ::std::os::raw::c_int, sample_rate: ::std::os::raw::c_int, vad_params: *const transcribe_vad_params, out_segments: *mut *mut transcribe_vad_segment, out_n_segments: *mut i64) -> transcribe_status {
    match crate::dynload::fn_ptr(17) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const f32, ::std::os::raw::c_int, ::std::os::raw::c_int, *const transcribe_vad_params, *mut *mut transcribe_vad_segment, *mut i64) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(pcm, n_samples, sample_rate, vad_params, out_segments, out_n_segments) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_free_vad` — index 18 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_free_vad(segments: *mut transcribe_vad_segment) -> () {
    match crate::dynload::fn_ptr(18) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_vad_segment) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(segments) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_run_params_init` — index 19 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_run_params_init(params: *mut transcribe_run_params) -> () {
    match crate::dynload::fn_ptr(19) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_run_params) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(params) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_capabilities_init` — index 20 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_capabilities_init(out: *mut transcribe_capabilities) -> () {
    match crate::dynload::fn_ptr(20) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_capabilities) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_model_get_capabilities` — index 21 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_get_capabilities(model: *const transcribe_model, out_caps: *mut transcribe_capabilities) -> transcribe_status {
    match crate::dynload::fn_ptr(21) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, *mut transcribe_capabilities) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, out_caps) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_model_supports` — index 22 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_supports(model: *const transcribe_model, feature: transcribe_feature) -> bool {
    match crate::dynload::fn_ptr(22) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, transcribe_feature) -> bool = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, feature) }
        }
        None => false,
    }
}

/// Runtime trampoline for `transcribe_model_arch_string` — index 23 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_arch_string(model: *const transcribe_model) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(23) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_model_variant_string` — index 24 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_variant_string(model: *const transcribe_model) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(24) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_model_backend` — index 25 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_backend(model: *const transcribe_model) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(25) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_model_meta_val_str` — index 26 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_meta_val_str(model: *const transcribe_model, key: *const ::std::os::raw::c_char) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(26) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, *const ::std::os::raw::c_char) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, key) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_model_load_file` — index 27 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_load_file(path: *const ::std::os::raw::c_char, params: *const transcribe_model_load_params, out_model: *mut *mut transcribe_model) -> transcribe_status {
    match crate::dynload::fn_ptr(27) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const ::std::os::raw::c_char, *const transcribe_model_load_params, *mut *mut transcribe_model) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(path, params, out_model) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_model_free` — index 28 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_model_free(model: *mut transcribe_model) -> () {
    match crate::dynload::fn_ptr(28) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_model) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_session_init` — index 29 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_session_init(model: *mut transcribe_model, params: *const transcribe_session_params, out_session: *mut *mut transcribe_session) -> transcribe_status {
    match crate::dynload::fn_ptr(29) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_model, *const transcribe_session_params, *mut *mut transcribe_session) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, params, out_session) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_session_free` — index 30 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_session_free(session: *mut transcribe_session) -> () {
    match crate::dynload::fn_ptr(30) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_open` — index 31 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_open(path: *const ::std::os::raw::c_char, load_params: *const transcribe_model_load_params, session_params: *const transcribe_session_params, out_session: *mut *mut transcribe_session) -> transcribe_status {
    match crate::dynload::fn_ptr(31) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const ::std::os::raw::c_char, *const transcribe_model_load_params, *const transcribe_session_params, *mut *mut transcribe_session) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(path, load_params, session_params, out_session) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_close` — index 32 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_close(session: *mut transcribe_session) -> () {
    match crate::dynload::fn_ptr(32) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_get_model` — index 33 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_model(session: *const transcribe_session) -> *const transcribe_model {
    match crate::dynload::fn_ptr(33) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> *const transcribe_model = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_run` — index 34 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_run(session: *mut transcribe_session, pcm: *const f32, n_samples: ::std::os::raw::c_int, params: *const transcribe_run_params) -> transcribe_status {
    match crate::dynload::fn_ptr(34) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, *const f32, ::std::os::raw::c_int, *const transcribe_run_params) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, pcm, n_samples, params) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_run_batch` — index 35 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_run_batch(session: *mut transcribe_session, pcm: *const *const f32, n_samples: *const ::std::os::raw::c_int, n: ::std::os::raw::c_int, params: *const transcribe_run_params) -> transcribe_status {
    match crate::dynload::fn_ptr(35) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, *const *const f32, *const ::std::os::raw::c_int, ::std::os::raw::c_int, *const transcribe_run_params) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, pcm, n_samples, n, params) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_set_abort_callback` — index 36 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_set_abort_callback(session: *mut transcribe_session, cb: transcribe_abort_callback, user_data: *mut ::std::os::raw::c_void) -> () {
    match crate::dynload::fn_ptr(36) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, transcribe_abort_callback, *mut ::std::os::raw::c_void) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, cb, user_data) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_set_progress_callback` — index 37 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_set_progress_callback(session: *mut transcribe_session, cb: transcribe_progress_callback, user_data: *mut ::std::os::raw::c_void) -> () {
    match crate::dynload::fn_ptr(37) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, transcribe_progress_callback, *mut ::std::os::raw::c_void) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, cb, user_data) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_was_aborted` — index 38 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_was_aborted(session: *const transcribe_session) -> bool {
    match crate::dynload::fn_ptr(38) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> bool = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => false,
    }
}

/// Runtime trampoline for `transcribe_was_truncated` — index 39 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_was_truncated(session: *const transcribe_session) -> bool {
    match crate::dynload::fn_ptr(39) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> bool = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => false,
    }
}

/// Runtime trampoline for `transcribe_session_limits_init` — index 40 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_session_limits_init(out: *mut transcribe_session_limits) -> () {
    match crate::dynload::fn_ptr(40) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session_limits) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_session_get_limits` — index 41 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_session_get_limits(session: *const transcribe_session, out: *mut transcribe_session_limits) -> transcribe_status {
    match crate::dynload::fn_ptr(41) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, *mut transcribe_session_limits) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_stream_params_init` — index 42 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_params_init(params: *mut transcribe_stream_params) -> () {
    match crate::dynload::fn_ptr(42) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_stream_params) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(params) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_stream_update_init` — index 43 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_update_init(out: *mut transcribe_stream_update) -> () {
    match crate::dynload::fn_ptr(43) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_stream_update) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_stream_text_init` — index 44 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_text_init(out: *mut transcribe_stream_text) -> () {
    match crate::dynload::fn_ptr(44) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_stream_text) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_stream_get_text` — index 45 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_get_text(session: *const transcribe_session, out: *mut transcribe_stream_text) -> transcribe_status {
    match crate::dynload::fn_ptr(45) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, *mut transcribe_stream_text) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_stream_begin` — index 46 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_begin(session: *mut transcribe_session, run_params: *const transcribe_run_params, stream_params: *const transcribe_stream_params) -> transcribe_status {
    match crate::dynload::fn_ptr(46) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, *const transcribe_run_params, *const transcribe_stream_params) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, run_params, stream_params) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_stream_feed` — index 47 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_feed(session: *mut transcribe_session, pcm: *const f32, n_samples: ::std::os::raw::c_int, update: *mut transcribe_stream_update) -> transcribe_status {
    match crate::dynload::fn_ptr(47) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, *const f32, ::std::os::raw::c_int, *mut transcribe_stream_update) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, pcm, n_samples, update) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_stream_finalize` — index 48 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_finalize(session: *mut transcribe_session, update: *mut transcribe_stream_update) -> transcribe_status {
    match crate::dynload::fn_ptr(48) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session, *mut transcribe_stream_update) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, update) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_stream_reset` — index 49 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_reset(session: *mut transcribe_session) -> () {
    match crate::dynload::fn_ptr(49) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_stream_get_state` — index 50 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_get_state(session: *const transcribe_session) -> transcribe_stream_state {
    match crate::dynload::fn_ptr(50) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> transcribe_stream_state = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => transcribe_stream_state(0),
    }
}

/// Runtime trampoline for `transcribe_stream_revision` — index 51 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_revision(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(51) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_stream_n_committed_segments` — index 52 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_n_committed_segments(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(52) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_stream_n_committed_words` — index 53 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_n_committed_words(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(53) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_stream_n_committed_tokens` — index 54 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_n_committed_tokens(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(54) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_stream_last_status` — index 55 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_stream_last_status(session: *const transcribe_session) -> transcribe_status {
    match crate::dynload::fn_ptr(55) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_tokenize` — index 56 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_tokenize(model: *const transcribe_model, text: *const ::std::os::raw::c_char, tokens: *mut i32, n_max: usize) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(56) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_model, *const ::std::os::raw::c_char, *mut i32, usize) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(model, text, tokens, n_max) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_timings_init` — index 57 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_timings_init(out: *mut transcribe_timings) -> () {
    match crate::dynload::fn_ptr(57) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_timings) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_get_timings` — index 58 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_timings(session: *const transcribe_session, out_timings: *mut transcribe_timings) -> transcribe_status {
    match crate::dynload::fn_ptr(58) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, *mut transcribe_timings) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, out_timings) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_print_timings` — index 59 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_print_timings(session: *const transcribe_session) -> () {
    match crate::dynload::fn_ptr(59) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_reset_timings` — index 60 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_reset_timings(session: *mut transcribe_session) -> () {
    match crate::dynload::fn_ptr(60) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_session) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_full_text` — index 61 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_full_text(session: *const transcribe_session) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(61) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_raw_text` — index 62 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_raw_text(session: *const transcribe_session) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(62) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_returned_timestamp_kind` — index 63 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_returned_timestamp_kind(session: *const transcribe_session) -> transcribe_timestamp_kind {
    match crate::dynload::fn_ptr(63) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> transcribe_timestamp_kind = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => transcribe_timestamp_kind(0),
    }
}

/// Runtime trampoline for `transcribe_n_segments` — index 64 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_n_segments(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(64) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_n_words` — index 65 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_n_words(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(65) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_n_tokens` — index 66 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_n_tokens(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(66) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_detected_language` — index 67 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_detected_language(session: *const transcribe_session) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(67) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_segment_init` — index 68 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_segment_init(out: *mut transcribe_segment) -> () {
    match crate::dynload::fn_ptr(68) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_segment) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_word_init` — index 69 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_word_init(out: *mut transcribe_word) -> () {
    match crate::dynload::fn_ptr(69) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_word) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_token_init` — index 70 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_token_init(out: *mut transcribe_token) -> () {
    match crate::dynload::fn_ptr(70) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_token) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_get_segment` — index 71 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_segment(session: *const transcribe_session, i: ::std::os::raw::c_int, out: *mut transcribe_segment) -> transcribe_status {
    match crate::dynload::fn_ptr(71) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_segment) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_get_word` — index 72 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_word(session: *const transcribe_session, i: ::std::os::raw::c_int, out: *mut transcribe_word) -> transcribe_status {
    match crate::dynload::fn_ptr(72) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_word) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_get_token` — index 73 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_token(session: *const transcribe_session, i: ::std::os::raw::c_int, out: *mut transcribe_token) -> transcribe_status {
    match crate::dynload::fn_ptr(73) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_token) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_speaker_segment_init` — index 74 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_speaker_segment_init(out: *mut transcribe_speaker_segment) -> () {
    match crate::dynload::fn_ptr(74) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_speaker_segment) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_n_speaker_segments` — index 75 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_n_speaker_segments(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(75) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_get_speaker_segment` — index 76 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_speaker_segment(session: *const transcribe_session, i: ::std::os::raw::c_int, out: *mut transcribe_speaker_segment) -> transcribe_status {
    match crate::dynload::fn_ptr(76) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_speaker_segment) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_n_results` — index 77 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_n_results(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(77) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_batch_status` — index 78 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_status(session: *const transcribe_session, i: ::std::os::raw::c_int) -> transcribe_status {
    match crate::dynload::fn_ptr(78) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_full_text` — index 79 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_full_text(session: *const transcribe_session, i: ::std::os::raw::c_int) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(79) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_batch_raw_text` — index 80 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_raw_text(session: *const transcribe_session, i: ::std::os::raw::c_int) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(80) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_batch_returned_timestamp_kind` — index 81 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_returned_timestamp_kind(session: *const transcribe_session, i: ::std::os::raw::c_int) -> transcribe_timestamp_kind {
    match crate::dynload::fn_ptr(81) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> transcribe_timestamp_kind = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => transcribe_timestamp_kind(0),
    }
}

/// Runtime trampoline for `transcribe_batch_detected_language` — index 82 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_detected_language(session: *const transcribe_session, i: ::std::os::raw::c_int) -> *const ::std::os::raw::c_char {
    match crate::dynload::fn_ptr(82) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> *const ::std::os::raw::c_char = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => ::std::ptr::null(),
    }
}

/// Runtime trampoline for `transcribe_batch_n_segments` — index 83 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_n_segments(session: *const transcribe_session, i: ::std::os::raw::c_int) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(83) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_batch_n_words` — index 84 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_n_words(session: *const transcribe_session, i: ::std::os::raw::c_int) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(84) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_batch_n_tokens` — index 85 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_n_tokens(session: *const transcribe_session, i: ::std::os::raw::c_int) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(85) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_batch_get_segment` — index 86 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_get_segment(session: *const transcribe_session, i: ::std::os::raw::c_int, j: ::std::os::raw::c_int, out: *mut transcribe_segment) -> transcribe_status {
    match crate::dynload::fn_ptr(86) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, ::std::os::raw::c_int, *mut transcribe_segment) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, j, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_get_word` — index 87 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_get_word(session: *const transcribe_session, i: ::std::os::raw::c_int, j: ::std::os::raw::c_int, out: *mut transcribe_word) -> transcribe_status {
    match crate::dynload::fn_ptr(87) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, ::std::os::raw::c_int, *mut transcribe_word) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, j, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_get_token` — index 88 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_get_token(session: *const transcribe_session, i: ::std::os::raw::c_int, j: ::std::os::raw::c_int, out: *mut transcribe_token) -> transcribe_status {
    match crate::dynload::fn_ptr(88) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, ::std::os::raw::c_int, *mut transcribe_token) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, j, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_n_speaker_segments` — index 89 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_n_speaker_segments(session: *const transcribe_session, i: ::std::os::raw::c_int) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(89) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_batch_get_speaker_segment` — index 90 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_get_speaker_segment(session: *const transcribe_session, i: ::std::os::raw::c_int, j: ::std::os::raw::c_int, out: *mut transcribe_speaker_segment) -> transcribe_status {
    match crate::dynload::fn_ptr(90) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, ::std::os::raw::c_int, *mut transcribe_speaker_segment) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, j, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_batch_get_timings` — index 91 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_batch_get_timings(session: *const transcribe_session, i: ::std::os::raw::c_int, out: *mut transcribe_timings) -> transcribe_status {
    match crate::dynload::fn_ptr(91) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_timings) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out) }
        }
        None => transcribe_status(8),
    }
}

/// Runtime trampoline for `transcribe_moonshine_streaming_stream_ext_init` — index 92 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_moonshine_streaming_stream_ext_init(ext: *mut transcribe_moonshine_streaming_stream_ext) -> () {
    match crate::dynload::fn_ptr(92) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_moonshine_streaming_stream_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_parakeet_stream_ext_init` — index 93 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_parakeet_stream_ext_init(ext: *mut transcribe_parakeet_stream_ext) -> () {
    match crate::dynload::fn_ptr(93) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_parakeet_stream_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_parakeet_buffered_stream_ext_init` — index 94 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_parakeet_buffered_stream_ext_init(ext: *mut transcribe_parakeet_buffered_stream_ext) -> () {
    match crate::dynload::fn_ptr(94) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_parakeet_buffered_stream_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_sortformer_stream_ext_init` — index 95 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_sortformer_stream_ext_init(ext: *mut transcribe_sortformer_stream_ext) -> () {
    match crate::dynload::fn_ptr(95) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_sortformer_stream_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_voxtral_realtime_stream_ext_init` — index 96 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_voxtral_realtime_stream_ext_init(ext: *mut transcribe_voxtral_realtime_stream_ext) -> () {
    match crate::dynload::fn_ptr(96) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_voxtral_realtime_stream_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_whisper_run_ext_init` — index 97 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_whisper_run_ext_init(ext: *mut transcribe_whisper_run_ext) -> () {
    match crate::dynload::fn_ptr(97) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_whisper_run_ext) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(ext) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_whisper_chunk_trace_init` — index 98 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_whisper_chunk_trace_init(out: *mut transcribe_whisper_chunk_trace) -> () {
    match crate::dynload::fn_ptr(98) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*mut transcribe_whisper_chunk_trace) -> () = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(out) }
        }
        None => (),
    }
}

/// Runtime trampoline for `transcribe_get_whisper_chunk_count` — index 99 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_whisper_chunk_count(session: *const transcribe_session) -> ::std::os::raw::c_int {
    match crate::dynload::fn_ptr(99) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session) -> ::std::os::raw::c_int = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session) }
        }
        None => -1,
    }
}

/// Runtime trampoline for `transcribe_get_whisper_chunk_trace` — index 100 in
/// [`TRANSCRIBE_DYN_SYMBOLS`].
#[no_mangle]
pub unsafe extern "C" fn transcribe_get_whisper_chunk_trace(session: *const transcribe_session, i: ::std::os::raw::c_int, out_trace: *mut transcribe_whisper_chunk_trace) -> transcribe_status {
    match crate::dynload::fn_ptr(100) {
        Some(__ptr) => {
            let __f: unsafe extern "C" fn(*const transcribe_session, ::std::os::raw::c_int, *mut transcribe_whisper_chunk_trace) -> transcribe_status = unsafe { ::std::mem::transmute(__ptr) };
            unsafe { __f(session, i, out_trace) }
        }
        None => transcribe_status(8),
    }
}

